package com.meridyen.billing;

/** Aylık faturaya ayrı taksit kalemi yazar (vergi kırılımı dahil). */
public class InvoiceLineWriter {

    /** Writes the monthly installment line onto the subscriber invoice. */
    public void writeInstallmentLine(String invoiceId, String planId) {
    }
}
