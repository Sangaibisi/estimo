[project]
name = "estimo-core"
version = "0.2.0"
description = "Estimo domain models: requirements, work items, estimate lines, BoE, ledger"
requires-python = ">=3.13"
license = "Apache-2.0"
dependencies = [
    "cryptography>=44",
    "pydantic>=2.13.4,<3",
]

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"
